$(document).ready(function () {
    const editBtn = $('.editBtn');
    const deleteBtn = $('.deleteBtn');
    var idMangaka;

    $.ajaxSetup({
        headers: {
            'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
        }
    });

    $('#formTambahMangaka').submit(function (e) { 
        e.preventDefault();
        var dataMangaka = new FormData($(this)[0])
        $.ajax({
            type: "POST",
            url: "/admin/mangaka",
            data: dataMangaka,
            contentType: false,
            processData: false,
            success: function (response) {
                $('#modalTambahMangaka').modal('hide');
                Swal.fire({
                    icon: 'success',
                    title: response.message,
                    text: 'Reloading the Page',
                    timer: 800,
                    timerProgressBar: true,
                    didOpen: function() {
                        Swal.showLoading();
                    },
                    willClose: function() {
                        location.reload();
                    }
                });
            },
            error: function (xhr, status, error) {
                Swal.fire({
                    icon: 'error',
                    title: 'Error!',
                    text: 'Terjadi kesalahan!'
                });
            }
        });
    });

    editBtn.on("click", function () {
        idMangaka = $(this).data("id");

        $.ajax({
            url: 'mangaka/' + idMangaka,
            type: 'GET',
            cache: false,
            success: function(response) {
                $("#idMangaka").val(response.data.id_mangaka);
                $("#editNamaMangaka").val(response.data.nama_mangaka);
            }
        });
    });

    $("#formEditMangaka").submit(function (e) { 
        e.preventDefault();
        var idMangaka = $("#idMangaka").val();
        var dataMangaka = new FormData($(this)[0]);

        $.ajax({
            type: "POST",
            url: "/admin/mangaka/" + idMangaka,
            data: dataMangaka,
            contentType: false,
            processData: false,
            headers: {
                "X-HTTP-Method-Override": "PUT"
            },
            success: function (response) {
                $("#modalEditMangaka").modal('hide');
                Swal.fire({
                    icon: 'success',
                    title: response.message,
                    text: 'Reloading the Page',
                    timer: 2100,
                    timerProgressBar: true,
                    didOpen: function() {
                        Swal.showLoading();
                    },
                    willClose: function() {
                        location.reload();
                    }
                });
            },
            error: function (xhr, status, error) {
                Swal.fire({
                    icon: 'error',
                    title: 'Error!',
                    text: 'Terjadi kesalahan!'
                });
            }
        });
    });

    deleteBtn.on('click', function(){
        idMangaka = $(this).data("id");

        Swal.fire({
            icon: 'warning',
            title: 'Apakah anda yakin mau menghapus data ini?',
            showCancelButton: true,
            confirmButtonText: 'Ya',
            cancelButtonText: 'Tidak',
            }).then((result) => {
                if (result.isConfirmed) {
                    $.ajax({
                        type: "DELETE",
                        url: "/admin/mangaka/" + idMangaka,
                        dataType: "json",
                        success: function (response) {
                            if (response.success) {
                                Swal.fire({
                                    icon: 'success',
                                    title: 'Data Buku Berhasil Dihapus!',
                                    text: 'Reloading the Page',
                                    timer: 2100,
                                    timerProgressBar: true,
                                    didOpen: function() {
                                        Swal.showLoading();
                                    },
                                    willClose: function() {
                                        location.reload();
                                    }
                                });
                            } else {
                                Swal.fire({
                                    icon: 'error',
                                    title: 'Error!',
                                    text: 'Data Buku Tidak Ditemukan!',
                                    timer: 2100,
                                    timerProgressBar: true,
                                });
                            }
                        }
                    });
                }
        });
    });
});